name = "flammel-api"
main = "src/index.js"
compatibility_date = "2024-11-01"

[[d1_databases]]
binding = "DB"
database_name = "flammel-orders"
database_id = "REPLACE_AFTER_CREATE"

[vars]
SHOP_NOTIFICATION_EMAIL = "flammel@flammel.cz"
MAIL_FROM = "flammel.cz <objednavky@flammel.cz>"

# RESEND_API_KEY is set as a Worker secret (wrangler secret put), not a var —
# see .github/workflows/deploy-worker.yml.
